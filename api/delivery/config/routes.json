{
  "routes": [
    {
      "method": "GET",
      "path": "/deliveries",
      "handler": "Delivery.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/deliveries/count",
      "handler": "Delivery.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/deliveries/:_id",
      "handler": "Delivery.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/deliveries",
      "handler": "Delivery.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/deliveries/:_id",
      "handler": "Delivery.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/deliveries/:_id",
      "handler": "Delivery.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}