{
  "connection": "default",
  "collectionName": "order",
  "info": {
    "name": "order",
    "description": "Order for clients\n"
  },
  "options": {
    "timestamps": true
  },
  "attributes": {
    "dateOrder": {
      "default": "",
      "type": "date"
    },
    "products": {
      "collection": "product",
      "via": "order"
    },
    "owner": {
      "model": "user",
      "via": "orders",
      "plugin": "users-permissions"
    },
    "status": {
      "default": "",
      "type": "enumeration",
      "enum": [
        "payed",
        "error",
        "processing"
      ]
    }
  }
}