{
  "connection": "default",
  "collectionName": "order",
  "info": {
    "name": "order",
    "description": "Order for clients\n"
  },
  "options": {
    "timestamps": true
  },
  "attributes": {
    "dateOrder": {
      "default": "",
      "type": "date"
    },
    "products": {
      "collection": "product",
      "via": "order"
    },
    "user": {
      "model": "user",
      "via": "order",
      "plugin": "users-permissions"
    },
    "status": {
      "default": "",
      "type": "enumeration",
      "enum": [
        "payed",
        "error",
        "processing"
      ]
    }
  }
}