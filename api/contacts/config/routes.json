{
  "routes": [
    {
      "method": "GET",
      "path": "/contacts",
      "handler": "Contacts.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contacts/count",
      "handler": "Contacts.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contacts/:_id",
      "handler": "Contacts.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/contacts",
      "handler": "Contacts.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/contacts/:_id",
      "handler": "Contacts.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/contacts/:_id",
      "handler": "Contacts.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
